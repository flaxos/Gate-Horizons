# Gate Horizons Audit Pack — Issue Ledger

## Fast health checks
- `python -m compileall -q .` → pass (no output).
- `pytest -q` → pass (`179 passed in 1.45s`).
- `python -m gate_horizons.tests.test_colony_logistics` → pass (`Ran 15 tests ... OK`), but demo output shows severe stability collapse for the hub colony by turn 20.

## Top 12 issues

| Severity | Category | File:Line | Symptom | Evidence (trace or grep snippet) | Likely Root Cause | Fix Plan | Verification Command |
|---|---|---|---|---|---|---|---|
| P1 | UI Wiring | `gate_horizons/ui/screens/galaxy_map.py:1151` | Ship context menu exposes actions that hard-stop at “not implemented yet”. | `elif action.name in {"Set Trade Route", "Prospect", "Set Auto-Mine"}: self._show_notice(f"{action.name} is not implemented yet...")` | UI action surface is ahead of implemented controller flow. | Either hide these actions until handlers exist, or implement handler routes in `GameState` and wire them here. | `rg -n "Set Trade Route|Prospect|Set Auto-Mine|not implemented yet" gate_horizons/ui/screens/galaxy_map.py` |
| P1 | Logic | `gate_horizons/ui/screens/system_view.py:793` | Gate activation path in System View bypasses `GameState.activate_gate()` (which applies tech cost reduction logic). | `system_view.py`: `self.game_state.galaxy.activate_gate(self.system_id, self.game_state.resources)` vs `state.py`: `activate_gate(... cost_reduction=tech_effects.get("gate_cost_reduction", 0.0))` | Two UI entrypoints call different gate-activation APIs. | Route System View to `self.game_state.activate_gate(self.system_id)` for parity with Galaxy Map. | `rg -n "activate_gate\(" gate_horizons/ui/screens/system_view.py gate_horizons/game/state.py gate_horizons/ui/screens/galaxy_map.py` |
| P1 | Logic | `gate_horizons/ui/screens/system_view.py:777` | Building ships from System View spends resources + spawns ships directly, bypassing shipyard production queue/timers/rules. | `_on_build_ship`: `resources.spend_dict(cost)` then `fleet.create_ship(...)` | Legacy shortcut path bypasses shipyard manager and its consistency checks. | Replace direct create with shipyard queue/build API (or remove shortcut from this screen). | `rg -n "def _on_build_ship|create_ship\(|spend_dict\(" gate_horizons/ui/screens/system_view.py` |
| P1 | UI Wiring | `gate_horizons/ui/screens/fleet_screen.py:255` | Freighter quick actions fail to appear for freighter variants (small/medium/large). | FleetScreen gates quick-action branch with `elif ship.ship_class == "freighter":` while other screens support variants. | Class filter in fleet UI is narrower than fleet/trade/logistics logic. | Align to variant-aware check used elsewhere (`in {freighter variants}` or contains `'freighter'`). | `rg -n "ship_class == \"freighter\"|small_freighter|medium_freighter|large_freighter" gate_horizons/ui/screens/fleet_screen.py gate_horizons/ui/screens/logistics_screen.py gate_horizons/game/ships.py` |
| P1 | UI Wiring | `gate_horizons/ui/screens/galaxy_map.py:1124` | “Emergency Stop” mutates ship path locally and may skip controller-level side effects/audit logging. | `ship.path.clear(); ship.destination=None; ship.mission=None` is done directly in UI handler. | UI directly mutates model object instead of dispatching action handler. | Add `GameState` handler for emergency stop and call through `issue_ship_order` / dispatch path. | `rg -n "Emergency Stop|ship\.path\.clear|issue_ship_order" gate_horizons/ui/screens/galaxy_map.py gate_horizons/game/state.py` |
| P2 | UI Wiring | `gate_horizons/ui/screens/trade_screen.py:358` | Manual manifest input parse errors are silently swallowed (no user feedback). | `except ValueError: pass` in route-creation input loop. | Validation failure path intentionally ignored. | Show inline validation error and block create until invalid fields are fixed. | `rg -n "except ValueError:\s*$|manifest_inputs" gate_horizons/ui/screens/trade_screen.py` |
| P2 | Persistence | `gate_horizons/game/state.py:794` | Encounter export filename mutates unexpectedly even when caller passes default contract filename. | `if filename == "EncounterSpec.json": filename = f"EncounterSpec_{encounter_id}.json"` | “Safe unique filename” behavior differs from static filename expectation. | Document this behavior in contract or keep default filename stable and make uniqueness opt-in. | `rg -n "EncounterSpec.json|EncounterSpec_" gate_horizons/game/state.py gate_horizons/ui/screens/encounter_screen.py` |
| P2 | Logic | `gate_horizons/game/state.py:1101` | Duplicate colony-colonist validation runs twice in same function. | Two identical `missing_colonists = self.get_missing_colonists(ship)` blocks at lines 1101 and 1105. | Merge artifact / copy-paste duplication. | Remove duplicate block to simplify flow and avoid drift bugs. | `rg -n "missing_colonists = self.get_missing_colonists" gate_horizons/game/state.py` |
| P1 | UI Wiring | `gate_horizons/ui/screens/galaxy_map.py:1080` | “Begin Mining” directly flips ship flags in UI without checks/constraints through controller. | `ship.mining = True; ship.mission = "mining"` | UI bypasses centralized action validation and report hooks. | Route mining start through a state handler (`issue_ship_order`-style). | `rg -n "Begin Mining|ship\.mining = True|issue_ship_order" gate_horizons/ui/screens/galaxy_map.py gate_horizons/game/state.py` |
| P2 | Balance | `README.md demo output` | Headless 30-turn logistics demo drives hub stability to `0` by turn 20 despite successful test pass. | Demo prints: `--- Turn 20 --- ... stability=0` and repeated `SHORTAGE at system_a: stability loss=20`. | Route policy in demo over-exports critical resources; balancing guardrails missing in preset. | Add safety thresholds in demo route manifest or stockpile reserve rules to avoid deterministic collapse. | `python -m gate_horizons.tests.test_colony_logistics` |
| P1 | UI Wiring | `gate_horizons/ui/screens/fleet_screen.py:397` | Fleet screen action execution depends on direct access to `app.galaxy_map_screen._execute_action`, creating tight coupling/single-point failure. | `_execute_action` delegates directly: `app.galaxy_map_screen._execute_action(ship_id, action)` | Screen-to-screen private method coupling instead of shared service/controller API. | Create shared action service on app/game_state and have both screens call that API. | `rg -n "galaxy_map_screen\._execute_action|def _execute_action" gate_horizons/ui/screens/fleet_screen.py gate_horizons/ui/screens/galaxy_map.py` |
| P2 | UI Wiring | `gate_horizons/ui/widgets/nav_menu.py:181` | Map options currently expose only one toggle action (“Toggle Flows”), leaving no direct map controls for other visual overlays despite separate systems existing. | `flow_item = NavDropDownItem(text="Toggle Flows")` as sole map option entry. | Navigation menu implementation lagging behind available map features. | Add additional map-option hooks (fog, influence, labels, etc.) or remove unused map systems from expectations. | `rg -n "Map Options|Toggle Flows|flow_toggle_callback" gate_horizons/ui/widgets/nav_menu.py` |

